# Retiro de resumenHome

Se elimina el campo opcional `resumenHome` del contenido institucional. La Home
vuelve a consumir exclusivamente `textoDescriptivo` desde el documento `inicio`
de Supabase, evitando duplicidad y ambigüedad entre dos fuentes editoriales.

También se retira su compatibilidad de la normalización pública.
